{
  "id": "time-summary",
  "name": "Time Summary Plugin",
  "description": "Local time + safe file read summary tools for OpenClaw automation.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "allowedRoots": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Whitelisted root paths for file access"
      },
      "maxReadBytes": {
        "type": "integer",
        "minimum": 256,
        "maximum": 1048576,
        "description": "Maximum bytes allowed for one read_and_summarize call"
      },
      "enableReadSummary": {
        "type": "boolean",
        "description": "Enable or disable read_and_summarize tool"
      }
    }
  },
  "uiHints": {
    "allowedRoots": {
      "label": "Allowed Roots",
      "help": "Only files under these directories can be read"
    },
    "maxReadBytes": {
      "label": "Max Read Bytes"
    },
    "enableReadSummary": {
      "label": "Enable Read Summary"
    }
  }
}
